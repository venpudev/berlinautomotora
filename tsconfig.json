{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "baseUrl": ".", // Le dice al compilador que la base para las rutas es el directorio raíz
    "paths": {
      "@/*": ["src/*"] // Define que todo lo que empiece con @/ se mapee a src/
    }
  }
}